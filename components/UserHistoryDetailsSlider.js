import React, { useEffect, useState } from 'react';
import { Drawer, Timeline, Spin } from 'antd';
import { getUserDetailsHistoryApi } from '../config';

const UserHistoryDetailsSlider = ({ versionId, open, onClose }) => {
const UserHistoryDetailsSlider = () => {
  return (
    <div>UserHistoryDetailsSlider</div>
  )
}

export default UserHistoryDetailsSlider